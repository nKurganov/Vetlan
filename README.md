# Vetlan Trading Bot

Автоматизированный торговый бот для Bybit, использующий стратегию на основе RSI, EMA, ATR и свечных паттернов.

## 📋 Содержание

- [Установка](#установка)
- [Настройка](#настройка)
- [Параметры конфигурации](#параметры-конфигурации)
- [Запуск](#запуск)
- [Стратегия](#стратегия)
- [Частота срабатывания ордеров](#-частота-срабатывания-ордеров)
- [Уведомления](#уведомления)
- [Статистика](#статистика)

## 🚀 Установка

1. Клонируйте репозиторий или скачайте файлы
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

**Примечание:** Виртуальное окружение не обязательно, если зависимости установлены глобально. Если хотите использовать venv для изоляции зависимостей, создайте его перед установкой:
   ```bash
   python -m venv venv
   venv\Scripts\activate  # Windows
   pip install -r requirements.txt
   ```

## ⚙️ Настройка

### 1. Создайте файл `.env` в корне проекта:

```env
BYBIT_API_KEY=ваш_api_ключ
BYBIT_API_SECRET=ваш_api_секрет
BYBIT_ENV=mainnet
TELEGRAM_TOKEN=ваш_telegram_токен
TELEGRAM_CHAT_ID=ваш_chat_id
```

**Важно:**
- `BYBIT_ENV` может быть `mainnet` или `testnet`
- Для тестирования используйте `testnet`
- API ключи можно получить в настройках аккаунта Bybit

### 2. Настройте параметры в `config/bybit_config.py`

Все параметры описаны ниже в разделе [Параметры конфигурации](#параметры-конфигурации).

## 📊 Параметры конфигурации

### 🔐 Подключение к бирже

| Параметр | Описание | Пример |
|----------|----------|--------|
| `api_key` | API ключ от Bybit (из `.env`) | Автоматически загружается |
| `api_secret` | API секрет от Bybit (из `.env`) | Автоматически загружается |
| `environment` | Среда торговли: `mainnet` или `testnet` | `testnet` для тестирования |

### 📈 Торговые инструменты

| Параметр | Описание | Рекомендуемое значение |
|----------|----------|------------------------|
| `interval` | Таймфрейм свечей (минуты) | `"15"` (15 минут) |
| `coins` | Список торговых пар | `["LINKUSDT", "DOGEUSDT", ...]` |

**Доступные интервалы:** `"1"`, `"3"`, `"5"`, `"15"`, `"30"`, `"60"`, `"120"`, `"240"`, `"360"`, `"720"`, `"D"`, `"W"`, `"M"`

### 📊 Индикаторы

#### RSI (Relative Strength Index)

| Параметр | Описание | Рекомендуемое значение |
|----------|----------|------------------------|
| `rsi_period` | Период расчёта RSI | `14` |
| `rsi_buy` | Порог RSI для открытия LONG | `25` (перепроданность) |
| `rsi_sell` | Порог RSI для открытия SHORT | `75` (перекупленность) |

**Логика:**
- LONG открывается при RSI < `rsi_buy` (рынок перепродан)
- SHORT открывается при RSI > `rsi_sell` (рынок перекуплен)

#### EMA (Exponential Moving Average)

| Параметр | Описание | Рекомендуемое значение |
|----------|----------|------------------------|
| `ema_period` | Период EMA | `50` |

**Использование:**
- Используется для фильтрации тренда (если `use_trend_filter: True`)
- LONG: цена должна быть **выше** EMA50 (восходящий тренд)
- SHORT: цена должна быть **ниже** EMA50 (нисходящий тренд)

#### ATR (Average True Range)

| Параметр | Описание | Рекомендуемое значение |
|----------|----------|------------------------|
| `atr_period` | Период расчёта ATR | `14` |
| `atr_min` | Минимальное значение ATR (не используется) | `0.0005` |
| `atr_max` | Максимальное значение ATR (не используется) | `0.02` |
| `min_atr_pct` | Минимальная волатильность в % от цены | `0.3` (0.3%) |

**Использование:**
- Используется для расчёта TP и SL
- Фильтр волатильности: не торгует при волатильности ниже `min_atr_pct`

#### Объём

| Параметр | Описание | Рекомендуемое значение |
|----------|----------|------------------------|
| `volume_sma` | Период SMA для объёма | `20` |
| `volume_mult` | Множитель объёма для фильтра | `1.5` |

**Логика:**
- Текущий объём должен быть ≥ SMA(объём) × `volume_mult`
- Исключает входы при слабой ликвидности

### 🎯 Take Profit и Stop Loss

| Параметр | Описание | Рекомендуемое значение |
|----------|----------|------------------------|
| `tp_long_atr` | Множитель ATR для TP в LONG | `2.5` |
| `sl_long_atr` | Множитель ATR для SL в LONG | `1.2` |
| `tp_short_atr` | Множитель ATR для TP в SHORT | `2.5` |
| `sl_short_atr` | Множитель ATR для SL в SHORT | `1.2` |
| `min_tp_pct` | Минимальный TP в % от цены входа | `0.01` (1%) |
| `min_sl_pct` | Минимальный SL в % от цены входа | `0.01` (1%) |

**Расчёт уровней:**

**LONG:**
- TP = max(entry + ATR × `tp_long_atr`, entry × (1 + `min_tp_pct`))
- SL = min(entry - ATR × `sl_long_atr`, entry × (1 - `min_sl_pct`))

**SHORT:**
- TP = min(entry - ATR × `tp_short_atr`, entry × (1 - `min_tp_pct`))
- SL = max(entry + ATR × `sl_short_atr`, entry × (1 + `min_sl_pct`))

### 🎛️ Включение/отключение функций

| Параметр | Описание | Значения |
|----------|----------|----------|
| `enable_long` | Разрешить открытие LONG позиций | `True` / `False` |
| `enable_short` | Разрешить открытие SHORT позиций | `True` / `False` |
| `enable_tp_sl` | Автоматически устанавливать TP/SL | `True` / `False` |
| `enable_patterns` | Проверять свечные паттерны | `True` / `False` |
| `use_trend_filter` | Использовать EMA50 для фильтра тренда | `True` / `False` |

**Паттерны:**
- **Spring** (для LONG): ложный пробой вниз - длинная нижняя тень, закрытие выше середины свечи
- **Upthrust** (для SHORT): ложный пробой вверх - длинная верхняя тень, закрытие ниже середины свечи

### 💰 Управление рисками

| Параметр | Описание | Рекомендуемое значение |
|----------|----------|------------------------|
| `risk_pct` | Риск на сделку в % от баланса | `2` (2%) |
| `min_order_usdt` | Минимальный размер ордера в USDT | `5` |
| `max_position_pct` | Максимальный размер позиции в % от баланса | `10` (10%) |

**Расчёт размера позиции:**
1. Риск в USDT = Баланс × `risk_pct` / 100
2. Размер позиции = Риск в USDT / Расстояние до SL
3. Ограничение: размер позиции ≤ Баланс × `max_position_pct` / 100

### 📱 Telegram уведомления

| Параметр | Описание | Где взять |
|----------|----------|-----------|
| `telegram_token` | Токен Telegram бота (из `.env`) | @BotFather в Telegram |
| `telegram_chat_id` | ID чата для уведомлений (из `.env`) | @userinfobot в Telegram |

**Как получить:**
1. Создайте бота через @BotFather
2. Получите токен
3. Узнайте свой chat_id через @userinfobot
4. Добавьте в `.env`

## 🚀 Запуск

### Проверка подключения

```bash
python main.py
```

Проверяет:
- Подключение к Bybit
- Корректность API ключей
- Доступность баланса
- Получение данных по свечам

### Запуск стратегии

```bash
python run_strategy.py
```

Бот начнёт:
- Анализировать указанные монеты каждые 30 секунд
- Открывать позиции при выполнении условий
- Отправлять уведомления в Telegram
- Логировать статистику

**Остановка:** `Ctrl+C`

## 📈 Стратегия

### Условия для открытия LONG

1. ✅ `enable_long = True`
2. ✅ RSI < `rsi_buy` (перепроданность)
3. ✅ Объём ≥ SMA(объём) × `volume_mult`
4. ✅ Волатильность ≥ `min_atr_pct`%
5. ✅ (Если `use_trend_filter = True`) Цена > EMA50
6. ✅ (Если `enable_patterns = True`) Есть паттерн Spring

### Условия для открытия SHORT

1. ✅ `enable_short = True`
2. ✅ RSI > `rsi_sell` (перекупленность)
3. ✅ Объём ≥ SMA(объём) × `volume_mult`
4. ✅ Волатильность ≥ `min_atr_pct`%
5. ✅ (Если `use_trend_filter = True`) Цена < EMA50
6. ✅ (Если `enable_patterns = True`) Есть паттерн Upthrust

### Защита от дублирования

- Бот не откроет повторную позицию по той же монете
- Проверяет открытые позиции перед каждым анализом
- Использует кэш для оптимизации запросов

## ⏱️ Частота срабатывания ордеров

### Текущие настройки (консервативные)

С текущими параметрами ордера будут срабатывать **редко** (1-5 ордеров в день или реже).

**Причины низкой частоты:**
- RSI > 75 встречается редко (обычно RSI находится в диапазоне 30-70)
- Паттерн Upthrust формируется не на каждой свече
- Фильтр тренда отсекает часть сигналов
- Фильтр объёма требует повышенной активности

### Как увеличить частоту сигналов

#### Вариант 1: Умеренная частота (5-15 ордеров в день)

```python
"rsi_sell": 70,              # Было 75, стало 70 (менее экстремально)
"volume_mult": 1.2,          # Было 1.5, стало 1.2 (менее строгий фильтр)
"enable_patterns": False,    # Отключить паттерны (самое большое влияние)
"min_atr_pct": 0.2,          # Было 0.3, стало 0.2 (меньше волатильности)
```

#### Вариант 2: Высокая частота (15-30+ ордеров в день)

```python
"rsi_sell": 65,              # Ещё менее экстремально
"volume_mult": 1.0,          # Убрать фильтр объёма
"enable_patterns": False,    # Отключить паттерны
"use_trend_filter": False,   # Отключить фильтр тренда
"min_atr_pct": 0.1,          # Минимальная волатильность
```

#### Вариант 3: Очень высокая частота (30+ ордеров в день)

```python
"rsi_sell": 60,              # Почти средний уровень
"volume_mult": 0.8,          # Обратный фильтр (торговать при низком объёме)
"enable_patterns": False,
"use_trend_filter": False,
"min_atr_pct": 0.05,         # Очень низкая волатильность
```

### Компромисс: частота vs качество

| Частота | RSI порог | Паттерны | Фильтр тренда | Качество сигналов |
|---------|-----------|----------|---------------|-------------------|
| **Низкая** (1-5/день) | 75 | Включены | Включен | ⭐⭐⭐⭐⭐ Очень высокое |
| **Средняя** (5-15/день) | 70 | Выключены | Включен | ⭐⭐⭐⭐ Высокое |
| **Высокая** (15-30/день) | 65 | Выключены | Выключен | ⭐⭐⭐ Среднее |
| **Очень высокая** (30+/день) | 60 | Выключены | Выключен | ⭐⭐ Низкое |

### Рекомендации

1. **Для начала:** Оставьте текущие настройки и понаблюдайте 1-2 недели
2. **Если сигналов слишком мало:** Снизьте `rsi_sell` до 70 и отключите паттерны
3. **Если сигналов слишком много:** Верните `rsi_sell` к 75 и включите все фильтры

### Частота проверки

Бот проверяет монеты **каждые 30 секунд** (настраивается в `run_strategy.py`). Это не означает, что ордера будут открываться каждые 30 секунд — они открываются только при выполнении всех условий стратегии.

## 📱 Уведомления

Бот отправляет уведомления в Telegram:

1. **При запуске:**
   - Баланс USDT
   - Список открытых позиций

2. **При открытии ордера:**
   - Символ
   - Направление (LONG/SHORT)
   - Цена входа
   - TP и SL уровни
   - Объём позиции

3. **При закрытии позиции:**
   - Символ
   - Размер позиции
   - Цена входа

4. **При остановке:**
   - Уведомление об остановке бота

## 📊 Статистика

Статистика сохраняется в `logs/stats.csv`:

| Колонка | Описание |
|---------|----------|
| Дата | Дата и время сделки |
| Символ | Торговая пара |
| Направление | LONG или SHORT |
| Цена входа | Цена открытия позиции |
| TP | Take Profit уровень |
| SL | Stop Loss уровень |
| Цена выхода | Цена закрытия позиции (если закрыта) |
| PnL (USDT) | Прибыль/убыток в USDT |
| ROI (%) | Доходность в процентах |
| Результат | Открыта / Прибыль / Убыток |

### Анализ статистики

Используйте `utils/stats_logger.py` для получения сводной статистики:

```python
from utils.stats_logger import StatsLogger

stats = StatsLogger()
summary = stats.get_summary()
print(summary)
```

**Доступные метрики:**
- `total_trades` - Всего сделок
- `open_trades` - Открытых позиций
- `closed_trades` - Закрытых позиций
- `wins` - Прибыльных сделок
- `losses` - Убыточных сделок
- `win_rate` - Процент прибыльных сделок
- `total_pnl` - Общий PnL
- `avg_win` - Средняя прибыль
- `avg_loss` - Средний убыток
- `profit_factor` - Профит-фактор

## 🔧 Структура проекта

```
vetlan/
├── config/
│   └── bybit_config.py      # Конфигурация бота
├── exchange/
│   └── bybit_client.py       # Клиент для работы с Bybit API
├── indicators/
│   └── indicators.py         # Индикаторы (RSI, EMA, ATR, паттерны)
├── orders/
│   └── order_manager.py      # Управление ордерами
├── strategy/
│   └── strategy.py           # Логика стратегии
├── utils/
│   ├── logger.py             # Настройка логирования
│   ├── notifier.py           # Telegram уведомления
│   └── stats_logger.py       # Логирование статистики
├── logs/
│   ├── bot.log               # Логи бота
│   └── stats.csv             # Статистика сделок
├── main.py                   # Проверка подключения
├── run_strategy.py           # Запуск стратегии
├── .env                      # Секретные ключи (создать самостоятельно)
└── README.md                 # Этот файл
```

## ⚠️ Важные замечания

1. **Тестирование:**
   - Всегда тестируйте на `testnet` перед использованием на реальных деньгах
   - Начните с минимальных сумм

2. **Риски:**
   - Торговля криптовалютами связана с рисками
   - Используйте только те средства, которые можете позволить себе потерять
   - Настройте `risk_pct` в соответствии с вашей толерантностью к риску

3. **API ключи:**
   - Никогда не публикуйте `.env` файл
   - Не давайте права на вывод средств API ключам
   - Используйте только права на торговлю

4. **Мониторинг:**
   - Регулярно проверяйте логи
   - Следите за балансом
   - Анализируйте статистику

## 🐛 Решение проблем

### Бот не открывает позиции

1. Проверьте баланс (должен быть достаточный USDT)
2. Проверьте условия стратегии (RSI, объём, волатильность)
3. Убедитесь, что `enable_long` или `enable_short` = `True`
4. Проверьте логи на наличие ошибок

### Ошибки подключения к Bybit

1. Проверьте API ключи в `.env`
2. Убедитесь, что `environment` указан правильно
3. Проверьте интернет-соединение
4. Проверьте статус API Bybit

### Telegram уведомления не приходят

1. Проверьте `telegram_token` и `telegram_chat_id` в `.env`
2. Убедитесь, что бот запущен (@BotFather)
3. Отправьте боту `/start` в личные сообщения

## 📝 Лицензия

Этот проект предназначен для образовательных целей. Используйте на свой риск.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в `logs/bot.log`
2. Проверьте конфигурацию в `config/bybit_config.py`
3. Убедитесь, что все зависимости установлены

---

**Удачной торговли! 🚀**

