# 🚀 Развертывание Vetlan Bot на Render.com

## 📋 Подготовка

Проект уже настроен для деплоя на Render.com. Все необходимые файлы созданы.

## 🎯 Шаги развертывания

### 1. Регистрация на Render.com

1. Перейдите на https://render.com
2. Нажмите **"Get Started for Free"**
3. Войдите через **GitHub** (рекомендуется)
4. Подтвердите доступ к репозиториям

### 2. Создание Background Worker

**ВАЖНО:** Используйте **Background Worker**, а не Web Service, так как бот должен работать постоянно!

1. В Dashboard нажмите **"New +"**
2. Выберите **"Background Worker"**
3. Подключите репозиторий:
   - **Connect account** → выберите GitHub
   - Выберите репозиторий: **nKurganov/Vetlan**
   - Branch: **main**

### 3. Настройка сервиса

Заполните форму:

- **Name:** `vetlan-trading-bot` (или любое имя)
- **Environment:** `Python 3`
- **Build Command:** 
  ```bash
  pip install -r requirements.txt
  ```
- **Start Command:**
  ```bash
  python run_strategy.py
  ```
- **Plan:** `Free` (или выберите платный для стабильности)

### 4. Настройка переменных окружения

В разделе **"Environment"** добавьте переменные:

| Key | Value | Описание |
|-----|-------|----------|
| `BYBIT_API_KEY` | ваш_api_ключ | API ключ от Bybit |
| `BYBIT_API_SECRET` | ваш_api_секрет | API секрет от Bybit |
| `BYBIT_ENV` | `mainnet` или `testnet` | Окружение (рекомендуется testnet для начала) |
| `TELEGRAM_TOKEN` | ваш_telegram_токен | Токен Telegram бота (опционально) |
| `TELEGRAM_CHAT_ID` | ваш_chat_id | ID чата для уведомлений (опционально) |

**Как добавить:**
1. Прокрутите вниз до секции **"Environment Variables"**
2. Нажмите **"Add Environment Variable"**
3. Введите Key и Value
4. Повторите для всех переменных

### 5. Деплой

1. Нажмите **"Create Background Worker"**
2. Render начнет деплой автоматически
3. Дождитесь завершения (обычно 2-5 минут)

### 6. Проверка работы

1. Откройте вкладку **"Logs"**
2. Вы должны увидеть:
   ```
   ========================================
              НАСТРОЙКИ БОТА
   ========================================
   ...
   🚀 Бот запущен. Ожидание сигналов...
   ```
3. Если видите логи работы бота — всё работает! ✅

## ⚙️ Автоматический деплой через render.yaml

Если хотите использовать автоматическую настройку:

1. В настройках сервиса → **"Settings"**
2. Включите **"Auto-Deploy"** (уже включено по умолчанию)
3. Render будет автоматически деплоить при каждом push в GitHub

## 🔧 Настройки для стабильной работы

### Рекомендуемые настройки:

1. **Auto-Deploy:** `Yes` (автоматический деплой при изменениях)
2. **Health Check Path:** оставьте пустым (для worker не нужен)
3. **Restart Policy:** `On Failure` (автоперезапуск при ошибках)

### Для платного тарифа ($7/месяц):

- ✅ Гарантированная работа 24/7 (не "засыпает")
- ✅ Больше ресурсов
- ✅ Приоритетная поддержка

## ⚠️ Важные замечания

### Бесплатный тариф Render.com:

- ⚠️ **Background Worker может "засыпать"** после 15 минут неактивности
- ⚠️ Для постоянной работы рекомендуется платный тариф
- ✅ Но для тестирования бесплатный тариф подойдет

### Если бот "засыпает":

1. Проверьте логи — возможно, есть ошибки
2. Рассмотрите переход на платный тариф ($7/месяц)
3. Или используйте альтернативу (Railway.app, Fly.io)

## 📊 Мониторинг

### Просмотр логов:

1. В Dashboard выберите ваш сервис
2. Откройте вкладку **"Logs"**
3. Логи обновляются в реальном времени

### Проверка работы:

- ✅ Логи показывают анализ монет
- ✅ Telegram уведомления приходят (если настроено)
- ✅ Статистика сохраняется (если есть доступ к файловой системе)

## 🔄 Обновление бота

После изменений в коде:

1. Закоммитьте изменения в GitHub:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```
2. Render автоматически задеплоит новую версию
3. Или нажмите **"Manual Deploy"** → **"Deploy latest commit"**

## 🆘 Решение проблем

### Бот не запускается:

1. **Проверьте логи** — там будет ошибка
2. **Проверьте переменные окружения** — все ли установлены
3. **Проверьте Build Command** — должен быть `pip install -r requirements.txt`
4. **Проверьте Start Command** — должен быть `python run_strategy.py`

### Ошибка импорта модулей:

- Убедитесь, что все зависимости в `requirements.txt`
- Проверьте, что Build Command выполнился успешно

### Бот останавливается:

- На бесплатном тарифе это нормально (может "засыпать")
- Проверьте логи на наличие ошибок
- Рассмотрите платный тариф для стабильности

## 🔒 Безопасность

**ВАЖНО:**
- ✅ Никогда не коммитьте `.env` файл
- ✅ Используйте Environment Variables в Render
- ✅ Не давайте права на вывод средств API ключам
- ✅ Используйте только права на торговлю

## 📝 Чек-лист перед деплоем

- [ ] Регистрация на Render.com
- [ ] Репозиторий подключен
- [ ] Создан Background Worker (не Web Service!)
- [ ] Установлены все переменные окружения
- [ ] Build Command: `pip install -r requirements.txt`
- [ ] Start Command: `python run_strategy.py`
- [ ] Деплой завершен успешно
- [ ] Логи показывают работу бота

## 🎉 Готово!

После успешного деплоя ваш бот будет работать на Render.com!

**Следующие шаги:**
1. Проверьте логи работы
2. Настройте мониторинг через Telegram
3. Следите за статистикой торговли

---

**Нужна помощь?** Проверьте логи в Render Dashboard или документацию: https://render.com/docs

