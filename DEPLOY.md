# 🚀 Инструкция по развертыванию бота на бесплатном сервере

## Вариант 1: Railway.app (Рекомендуется) ⭐

### Преимущества:
- ✅ $5 бесплатных кредитов в месяц
- ✅ Работает 24/7
- ✅ Автоматический деплой из GitHub
- ✅ Простая настройка

### Шаги:

1. **Зарегистрируйтесь на Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите репозиторий `nKurganov/Vetlan`

3. **Настройте переменные окружения:**
   - В настройках проекта → Variables
   - Добавьте все переменные из `.env`:
     ```
     BYBIT_API_KEY=ваш_ключ
     BYBIT_API_SECRET=ваш_секрет
     BYBIT_ENV=mainnet
     TELEGRAM_TOKEN=ваш_токен
     TELEGRAM_CHAT_ID=ваш_chat_id
     ```

4. **Настройте команду запуска:**
   - В настройках сервиса → Settings → Start Command
   - Укажите: `python run_strategy.py`

5. **Деплой:**
   - Railway автоматически задеплоит проект
   - Бот начнет работать

---

## Вариант 2: Render.com ⭐ (Выбранный вариант)

**📖 Подробная инструкция:** См. файл [RENDER_DEPLOY.md](RENDER_DEPLOY.md)

### Быстрый старт:

1. **Зарегистрируйтесь:** https://render.com
2. **Создайте Background Worker** (НЕ Web Service!)
   - New → Background Worker
   - Подключите GitHub репозиторий: `nKurganov/Vetlan`
3. **Настройки:**
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python run_strategy.py`
   - **Environment:** Python 3
4. **Добавьте переменные окружения:**
   - `BYBIT_API_KEY`
   - `BYBIT_API_SECRET`
   - `BYBIT_ENV` (mainnet/testnet)
   - `TELEGRAM_TOKEN` (опционально)
   - `TELEGRAM_CHAT_ID` (опционально)
5. **Создайте сервис** — деплой начнется автоматически

**⚠️ Важно:** Используйте **Background Worker**, а не Web Service, так как бот должен работать постоянно!

---

## Вариант 3: Fly.io

### Шаги:

1. **Установите Fly CLI:**
   ```bash
   powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
   ```

2. **Войдите:**
   ```bash
   fly auth login
   ```

3. **Создайте приложение:**
   ```bash
   fly launch
   ```

4. **Настройте переменные:**
   ```bash
   fly secrets set BYBIT_API_KEY=ваш_ключ
   fly secrets set BYBIT_API_SECRET=ваш_секрет
   fly secrets set BYBIT_ENV=mainnet
   fly secrets set TELEGRAM_TOKEN=ваш_токен
   fly secrets set TELEGRAM_CHAT_ID=ваш_chat_id
   ```

5. **Деплой:**
   ```bash
   fly deploy
   ```

---

## Вариант 4: Oracle Cloud Always Free

### Преимущества:
- ✅ Полностью бесплатно навсегда
- ✅ Полный контроль
- ✅ Работает 24/7

### Шаги:

1. **Зарегистрируйтесь:** https://www.oracle.com/cloud/free

2. **Создайте VM:**
   - Compute → Instances → Create Instance
   - Выберите Always Free-eligible shape
   - Ubuntu 22.04

3. **Подключитесь по SSH:**
   ```bash
   ssh ubuntu@ваш_ip
   ```

4. **Установите зависимости:**
   ```bash
   sudo apt update
   sudo apt install python3-pip git -y
   ```

5. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/nKurganov/Vetlan.git
   cd Vetlan
   ```

6. **Установите зависимости:**
   ```bash
   pip3 install -r requirements.txt
   ```

7. **Создайте .env файл:**
   ```bash
   nano .env
   # Вставьте ваши ключи
   ```

8. **Запустите через screen/tmux:**
   ```bash
   sudo apt install screen -y
   screen -S bot
   python3 run_strategy.py
   # Нажмите Ctrl+A, затем D для отсоединения
   ```

---

## Вариант 5: Amvera Cloud (Российский)

### Шаги:

1. **Зарегистрируйтесь:** https://amvera.ru

2. **Создайте приложение:**
   - Подключите GitHub репозиторий
   - Выберите Python

3. **Настройте переменные окружения**

4. **Деплой автоматический**

---

## 🔒 Безопасность

**ВАЖНО:** При использовании облачных сервисов:
- ✅ Используйте переменные окружения (не коммитьте .env)
- ✅ Не давайте права на вывод средств API ключам
- ✅ Используйте только права на торговлю
- ✅ Регулярно проверяйте логи

---

## 📊 Мониторинг

После деплоя проверьте:
- Логи работы бота
- Статистику в Telegram
- Баланс на бирже
- Открытые позиции

---

## 🆘 Решение проблем

### Бот не запускается:
- Проверьте логи в панели управления
- Убедитесь, что все переменные окружения установлены
- Проверьте, что зависимости установлены

### Бот останавливается:
- На бесплатных тарифах могут быть ограничения
- Используйте мониторинг и автоперезапуск
- Рассмотрите платный тариф для стабильности

---

## 💡 Рекомендации

**Для начала:** Railway.app - самый простой вариант
**Для постоянной работы:** Oracle Cloud Always Free - полностью бесплатно
**Для тестирования:** Render.com или Fly.io

