# 🚀 Развертывание Vetlan Bot на Replit.com

## 📋 Подготовка

Проект настроен для работы на Replit.com. Все необходимые файлы созданы.

## 🎯 Шаги развертывания

### 1. Регистрация на Replit

1. Перейдите на https://replit.com
2. Нажмите **"Sign up"** или **"Log in"**
3. Войдите через **GitHub** (рекомендуется)
4. Подтвердите доступ к репозиториям

### 2. Импорт проекта из GitHub

1. В Dashboard нажмите **"Create Repl"** или **"+"**
2. Выберите **"Import from GitHub"**
3. Введите URL репозитория: `https://github.com/nKurganov/Vetlan`
4. Или выберите репозиторий из списка: `nKurganov/Vetlan`
5. Нажмите **"Import"**

### 3. Настройка переменных окружения

1. В Replit откройте ваш проект
2. В левой панели найдите иконку **"Secrets"** (🔒) или перейдите в **Tools → Secrets**
3. Добавьте следующие переменные:

| Key | Value | Описание |
|-----|-------|----------|
| `BYBIT_API_KEY` | ваш_api_ключ | API ключ от Bybit |
| `BYBIT_API_SECRET` | ваш_api_секрет | API секрет от Bybit |
| `BYBIT_ENV` | `mainnet` или `testnet` | Окружение (рекомендуется testnet для начала) |
| `TELEGRAM_TOKEN` | ваш_telegram_токен | Токен Telegram бота (опционально) |
| `TELEGRAM_CHAT_ID` | ваш_chat_id | ID чата для уведомлений (опционально) |

**Как добавить:**
1. Нажмите **"New secret"**
2. Введите Key (например, `BYBIT_API_KEY`)
3. Введите Value (ваш реальный ключ)
4. Нажмите **"Add secret"**
5. Повторите для всех переменных

### 4. Установка зависимостей

Replit автоматически установит зависимости из `requirements.txt` при первом запуске.

Если нужно установить вручную:
1. Откройте **Shell** (терминал внизу)
2. Выполните:
   ```bash
   pip install -r requirements.txt
   ```

### 5. Запуск бота

1. Нажмите кнопку **"Run"** (▶️) вверху
2. Или используйте команду в Shell:
   ```bash
   python run_strategy.py
   ```

### 6. Проверка работы

В консоли вы должны увидеть:
```
============================================================
              НАСТРОЙКИ БОТА
============================================================
...
🚀 Бот запущен. Ожидание сигналов...
```

## ⚙️ Настройка для постоянной работы

### Проблема: Replit "засыпает" на бесплатном тарифе

Replit останавливает выполнение после 1 часа неактивности на бесплатном тарифе.

### Решения:

#### Вариант 1: Использовать Always On (платно)

1. Перейдите в **Settings → Billing**
2. Включите **"Always On"** ($5/месяц)
3. Бот будет работать постоянно

#### Вариант 2: Использовать UptimeRobot (бесплатно)

1. Зарегистрируйтесь на https://uptimerobot.com
2. Создайте HTTP Monitor
3. Укажите URL вашего Replit (получите через **Share → Copy link**)
4. UptimeRobot будет "будить" Replit каждые 5 минут

#### Вариант 3: Использовать cron-job.org (бесплатно)

1. Зарегистрируйтесь на https://cron-job.org
2. Создайте задачу
3. URL: ваш Replit URL
4. Интервал: каждые 5 минут

#### Вариант 4: Использовать Python скрипт с keep-alive

Создайте простой веб-сервер для keep-alive (опционально):

```python
# keep_alive.py
from flask import Flask
from threading import Thread
import requests

app = Flask('')

@app.route('/')
def home():
    return "Bot is alive!"

def run():
    app.run(host='0.0.0.0', port=8080)

def keep_alive():
    t = Thread(target=run)
    t.daemon = True
    t.start()
```

## 🔧 Настройка проекта

### Файл .replit

Уже создан и настроен:
- Язык: Python 3
- Команда запуска: `python run_strategy.py`

### Файл replit.nix

Уже создан для установки зависимостей Python.

## 📊 Мониторинг

### Просмотр логов:

1. Логи отображаются в консоли Replit
2. Можно использовать вкладку **"Logs"** для истории

### Проверка работы:

- ✅ Логи показывают анализ монет
- ✅ Telegram уведомления приходят (если настроено)
- ✅ Статистика сохраняется в файлы (в рамках Replit)

## 🔄 Обновление бота

### Способ 1: Через GitHub

1. Внесите изменения локально
2. Закоммитьте и запушьте в GitHub:
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```
3. В Replit нажмите **"Version control"** → **"Pull latest"**

### Способ 2: Прямо в Replit

1. Редактируйте файлы в Replit
2. Изменения сохраняются автоматически
3. Перезапустите бота кнопкой **"Run"**

## 🆘 Решение проблем

### Бот не запускается:

1. **Проверьте логи** в консоли — там будет ошибка
2. **Проверьте переменные окружения** — все ли установлены в Secrets
3. **Проверьте зависимости** — выполните `pip install -r requirements.txt`
4. **Проверьте Python версию** — должна быть 3.11+

### Ошибка импорта модулей:

```bash
pip install -r requirements.txt
```

### Бот останавливается:

- На бесплатном тарифе Replit останавливает выполнение после 1 часа неактивности
- Используйте UptimeRobot или cron-job.org для keep-alive
- Или включите Always On ($5/месяц)

### Ошибка подключения к Bybit:

1. Проверьте API ключи в Secrets
2. Проверьте переменную `BYBIT_ENV` (mainnet/testnet)
3. Проверьте интернет-соединение в Replit

## 🔒 Безопасность

**ВАЖНО:**
- ✅ Используйте **Secrets** для хранения ключей (не в коде!)
- ✅ Никогда не коммитьте `.env` файл
- ✅ Не давайте права на вывод средств API ключам
- ✅ Используйте только права на торговлю

## 📝 Чек-лист перед запуском

- [ ] Регистрация на Replit.com
- [ ] Проект импортирован из GitHub
- [ ] Все переменные окружения установлены в Secrets
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] Бот запущен и работает
- [ ] Логи показывают работу бота
- [ ] Настроен keep-alive (если нужна постоянная работа)

## 💡 Полезные советы

### Для тестирования:

- Используйте `BYBIT_ENV=testnet` в Secrets
- Проверяйте работу на тестовой сети перед mainnet

### Для production:

- Используйте `BYBIT_ENV=mainnet`
- Включите Always On или настройте keep-alive
- Регулярно проверяйте логи
- Мониторьте баланс на бирже

## 🎉 Готово!

После успешного запуска ваш бот будет работать на Replit.com!

**Следующие шаги:**
1. Проверьте логи работы
2. Настройте keep-alive для постоянной работы
3. Настройте мониторинг через Telegram
4. Следите за статистикой торговли

---

**Нужна помощь?** 
- Документация Replit: https://docs.replit.com
- Сообщество: https://replit.com/talk

